# keyd config — migrated from AHK capslock.ahk
# CapsLock acts as Hyper (Ctrl+Alt+Shift) when held, CapsLock when tapped
# Install: sudo pacman -S keyd && sudo systemctl enable --now keyd

[ids]
*

[main]
capslock = overload(hyper, capslock)

[hyper:C-A-S]
